// next.config.js

module.exports = {
  i18n: {
    locales: ["en", "ro"], // Define supported languages
    defaultLocale: "en", // English as the default
    localeDetection: false, // Disable automatic locale detection
  },
};
